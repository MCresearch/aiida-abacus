repos:
- repo: local
  hooks:
  - id: format
    name: format
    entry: ruff format
    language: system
    types: [python]
  - id: lint
    name: lint
    entry: ruff check --fix
    language: system
    types: [python]
